name: Build & Release pipeline
on:
  push:

jobs:
  build_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: python version
        run: python --version